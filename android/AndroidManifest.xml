<?xml version="1.0" encoding="utf-8"?>
<!--
  Cliff Crack for Android: a NativeActivity with no Java of its own. The game is
  libcliffcrack.so (Go, built with -buildmode=c-shared), which loads
  librenderer.so (the C++ Vulkan renderer). See build-android.ps1.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cliffcrack.game">

    <!-- Vulkan 1.3 (the renderer uses dynamic rendering and synchronization2). -->
    <uses-feature android:name="android.hardware.vulkan.version" android:version="0x403000" android:required="true" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:label="Cliff Crack"
        android:hasCode="false"
        android:isGame="true"
        android:appCategory="game"
        android:extractNativeLibs="true"
        android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">

        <!-- Landscape either way up; rotating doesn't restart the activity (the
             renderer just rebuilds its swapchain). -->
        <activity
            android:name="android.app.NativeActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="sensorLandscape"
            android:resizeableActivity="false"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboard|keyboardHidden|navigation|uiMode|density">
            <meta-data android:name="android.app.lib_name" android:value="cliffcrack" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
